#ifndef TRAVERSE_H
#define TRAVERSE_H

#include "solutions.h"

bool traverse(int*, int, int*, const std::vector<int>*);
bool traverse_with_preserve(int*, int, int, int*, const std::vector<int>*);
bool traverse_preserved(int*, int, int*, const std::vector<int>*);
bool traverse_preserved_with_preserve(int*, int, int, int*, const std::vector<int>*);

#endif